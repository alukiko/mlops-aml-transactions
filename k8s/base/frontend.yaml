apiVersion: apps/v1
kind: Deployment
metadata:
  name: aml-frontend
  namespace: aml-monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aml-frontend
  template:
    metadata:
      labels:
        app: aml-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/alukiko/mlops-aml-transactions/aml-frontend:152a27289ff7bd019ec3e150971ddad3228e03da
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: aml-frontend
  namespace: aml-monitoring
spec:
  type: NodePort
  selector:
    app: aml-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
