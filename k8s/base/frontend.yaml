apiVersion: apps/v1
kind: Deployment
metadata:
  name: aml-frontend
  namespace: aml-monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aml-frontend
  template:
    metadata:
      labels:
        app: aml-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/alukiko/mlops-aml-transactions/aml-frontend:d108a76e83e8eb269a77c13c6f072b70139491f8
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: aml-frontend
  namespace: aml-monitoring
spec:
  type: NodePort
  selector:
    app: aml-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
